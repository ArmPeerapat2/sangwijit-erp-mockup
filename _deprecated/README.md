# Deprecated Files

ไฟล์ในโฟลเดอร์นี้เคยใช้งานจริงแต่ **ไม่ใช้แล้ว** เก็บไว้เป็น reference เผื่อดูย้อนหลัง

## รายการ

| ไฟล์ | เลิกใช้ | แทนที่ด้วย | หมายเหตุ |
|---|---|---|---|
| `portal-mockup-index.html` | 2026-04-22 | `index.html` (root) | v1 ของหน้า index · v2.0 ใหม่กว่ามี sidebar collapse + search + BC365 matrix + 64 pages · refs ภายในไฟล์อาจ stale |

## กติกา
- **ห้าม link กลับ** ไปไฟล์ใน `_deprecated/` จากไฟล์ active
- ถ้าจะ resurrect ต้องย้ายกลับ root + audit stale refs ก่อน
- ลบได้ถ้าไม่ต้องการเก็บประวัติ (git log เก็บไว้แล้ว)
