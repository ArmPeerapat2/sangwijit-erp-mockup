{
  "name": "sangwijit-erp-mockup",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "Independent ERP mockup redesign workspace",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "node scripts/validate-workspace.mjs",
    "lint": "node scripts/lint.mjs",
    "test": "node --test tests/**/*.test.mjs",
    "typecheck": "node scripts/validate-workspace.mjs"
  }
}
